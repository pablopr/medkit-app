# Vercel Python Functions install dependencies from the project root.
# Keep this in sync with backend/requirements.txt.

fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
livekit-api~=0.8
httpx==0.28.1
slowapi==0.1.9
